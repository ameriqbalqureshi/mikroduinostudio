{
  "name": "@mikroduino/project-manager",
  "version": "0.1.0",
  "description": "MikroDuino project file management — create, open, save, validate .mdp projects",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@mikroduino/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
