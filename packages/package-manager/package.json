{
  "name": "@mikroduino/package-manager",
  "version": "0.1.0",
  "description": "MikroDuino library package manager — install, remove, update, search packages",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@mikroduino/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
